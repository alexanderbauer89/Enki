Write the attribute/value pair (both expressed as Strings) to the view object:

view.???(???,
    Charset.defaultCharset()
            .???);

encode(attr)
encode(val)
encode
attr
val
write

> write
> attr
> encode(val)