Given the code with missing parts, and the description, make the code match the description.

???.groupby('???')???\
  ???(ascending=False)\
  .reset_index(name='???')
  
Group the people, DataFrame by its Age column. Count the number of rows for each Age group. 
Sort the values in descending order. 
Reset the index to a new logical one from 0 to length and add a new column called Count By Age where the groupby.size will display the output.

age
count by age
.sort_values
Age
Count By Age

size()
people

> people
> Age
> .size()
> .sort_values
> Count By Age
